---
phase: 01-runtime-core-pipeline
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - lib/core/constants/model_config.dart
  - lib/runtime/edge_veda_runtime.dart
  - lib/features/ocr/data/ocr_service.dart
  - lib/features/ocr/presentation/ocr_test_screen.dart
autonomous: false

must_haves:
  truths:
    - "OCR inference runs on Metal GPU (useGpu: true confirmed in VisionWorker init)"
    - "App does not crash during inference on physical iPhone 13+ device"
    - "Extracted text from a known test image is recognizable and coherent (not garbage)"
    - "ChatTemplateFormat produces correct output (not random tokens or repeated patterns)"
    - "Memory stays within safe bounds during inference (app not killed by iOS jetsam)"
  artifacts:
    - path: "lib/runtime/edge_veda_runtime.dart"
      provides: "Validated VisionWorker configuration (correct ChatTemplateFormat, GPU enabled)"
      contains: "useGpu: true"
    - path: "lib/core/constants/model_config.dart"
      provides: "Verified model file names and URLs that actually download correctly"
      contains: "modelUrl"
  key_links:
    - from: "lib/runtime/edge_veda_runtime.dart"
      to: "VisionWorker.initVision"
      via: "Correct parameters validated on device"
      pattern: "initVision"
    - from: "lib/features/ocr/data/ocr_service.dart"
      to: "VisionWorker.describeFrame"
      via: "Inference call with validated prompt"
      pattern: "describeFrame"
---

<objective>
Validate the entire pipeline on a physical iOS device: model download completes, VisionWorker initializes with Metal GPU, inference produces coherent OCR text, and memory stays within safe bounds. Fix any integration issues discovered during device testing.

Purpose: This is the "moment of truth" for Phase 1. Simulator cannot validate Metal GPU, real memory pressure, or actual model inference quality. The research warns that wrong ChatTemplateFormat produces garbage output -- this plan confirms it works or fixes it.

Output: A validated, working OCR pipeline on a physical iPhone 13+ device with confirmed Metal GPU acceleration and coherent text extraction.
</objective>

<execution_context>
@C:/Users/prave/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/prave/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-runtime-core-pipeline/01-RESEARCH.md
@.planning/phases/01-runtime-core-pipeline/01-01-SUMMARY.md
@.planning/phases/01-runtime-core-pipeline/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add diagnostic logging and prepare device validation build</name>
  <files>
    lib/runtime/edge_veda_runtime.dart
    lib/features/ocr/data/ocr_service.dart
    lib/features/ocr/presentation/ocr_test_screen.dart
    lib/core/constants/model_config.dart
  </files>
  <action>
    1. Add diagnostic logging to `lib/runtime/edge_veda_runtime.dart`:
       - Log VisionWorker.spawn() success/failure with timing
       - Log initVision() parameters (modelPath, mmprojPath, numThreads, contextSize, useGpu) and success/failure with timing
       - Log model file sizes on disk before initVision (verify download integrity)
       - Use `dart:developer` log() for debug output visible in Xcode console
       - If initVision fails, log the error message in detail -- this is where ChatTemplateFormat issues surface

    2. Add diagnostic logging to `lib/features/ocr/data/ocr_service.dart`:
       - Log preprocessing time (ms)
       - Log image dimensions after resize (should be <= 1024px longest edge)
       - Log RGB byte array size (should equal width * height * 3)
       - Log inference start, completion time, and output text length
       - Log first 200 chars of output (to detect garbage output patterns)

    3. Update `lib/features/ocr/presentation/ocr_test_screen.dart` to show diagnostic info in the complete state:
       - Processing time breakdown if available
       - Image dimensions used for inference
       - Model file status (both files present, file sizes)
       - Add a small debug info section below the extracted text (collapsible or small text)

    4. Verify model file URLs in `lib/core/constants/model_config.dart`:
       - Ensure HuggingFace URLs use `/resolve/main/` format for direct download
       - Add a comment noting the exact file sizes expected
       - If mmproj filename was estimated, add a TODO noting it needs device verification

    5. Build release mode for device: `flutter build ios --release` (or prepare for `flutter run --release` on connected device)

    CRITICAL VALIDATION ITEMS (from research pitfalls):
    - Pitfall 1 (ChatTemplateFormat): If output is garbage (random chars, repeated tokens, unrelated text), VisionWorker may need explicit ChatTemplateFormat. describeFrame takes a plain prompt -- if it auto-detects, great. If not, try creating a ChatSession with different ChatTemplateFormat values.
    - Pitfall 2 (Memory): Watch Xcode memory gauge during inference. Target < 1.5 GB peak. If approaching 2 GB, reduce contextSize or image dimensions.
    - Pitfall 4 (Two model files): Both GGUF + mmproj must exist. Missing mmproj = vision inference fails silently.
    - Pitfall 6 (RGB format): Wrong byte order = model "sees" wrong image. Verify with a simple test image.

    If any ChatTemplateFormat or API parameter changes are needed based on error logs, make those fixes in this task.
  </action>
  <verify>
    - `flutter analyze` reports no errors
    - `flutter build ios --release --no-codesign` succeeds
    - Diagnostic log() calls are present in runtime and OCR service
    - OcrTestScreen shows debug info section
  </verify>
  <done>
    Release build compiles. Diagnostic logging added to runtime init, inference, and test screen. Model file URLs verified. Ready for physical device testing.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Physical device validation of complete OCR pipeline</name>
  <files>
    lib/runtime/edge_veda_runtime.dart
    lib/core/constants/model_config.dart
  </files>
  <action>
    CHECKPOINT: Human must run the app on a physical iPhone 13+ device and verify the complete pipeline.
    Claude has automated everything possible (diagnostic logging, release build). This checkpoint validates
    what only a physical device can confirm: Metal GPU acceleration, real memory pressure, actual model
    inference quality, and download resume behavior.

    If the human reports issues (garbage output, crashes, memory kills), Claude will fix the identified
    files (edge_veda_runtime.dart for ChatTemplateFormat, model_config.dart for URLs, ocr_service.dart
    for preprocessing) before the checkpoint is marked complete.
  </action>
  <verify>
    Human confirms all 6 validation checks pass on physical iPhone 13+ device:
    1. Fresh install: download screen shows progress (percentage + MB), completes successfully
    2. Resume: force-kill during download, relaunch resumes from where it stopped
    3. OCR inference: pick image with clear text, extracted text is recognizable
    4. No garbage: output is coherent (confirms ChatTemplateFormat is correct)
    5. Memory: app stays under ~1.5 GB, no iOS jetsam kills
    6. Sequential: second inference works without re-init or crash
  </verify>
  <done>
    Physical device validation passes all 6 checks. Phase 1 success criteria confirmed:
    OCR-01 (extract text), OCR-02 (Metal GPU), OCR-05 (download with progress/resume),
    RT-01 (no network for OCR), RT-02 (Metal GPU acceleration).
  </done>
</task>

</tasks>

<verification>
Physical device validation confirms:
1. Model download completes with visible progress (OCR-05)
2. Download resumes after interruption (OCR-05)
3. VisionWorker initializes with Metal GPU (OCR-02, RT-02)
4. Inference produces coherent text from a real image (OCR-01)
5. No network calls during OCR processing (RT-01)
6. App does not crash or get killed during inference (memory within bounds)
7. Second inference works without re-initialization (VisionWorker persists)
</verification>

<success_criteria>
- Physical device runs complete pipeline: download -> init -> preprocess -> infer -> display text
- Extracted text is recognizable (not garbage -- confirms ChatTemplateFormat)
- Metal GPU enabled (useGpu: true in initVision)
- Memory stays under iOS jetsam threshold
- Download resume works after app kill
- Multiple inferences work in sequence without crash
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-core-pipeline/01-03-SUMMARY.md`
</output>
