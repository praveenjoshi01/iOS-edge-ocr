import 'package:flutter/material.dart';

/// Placeholder download screen. Full implementation in Task 2.
class DownloadScreen extends StatelessWidget {
  const DownloadScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Edge OCR Setup')),
      body: const Center(
        child: Text('Download Screen - Task 2 will implement'),
      ),
    );
  }
}
